# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ค่า key จริง (อย่า commit .env.local ขึ้น git)

# Typhoon (SCB 10X) — ขอที่ https://opentyphoon.ai  (ใช้ใน B6 แชต)
TYPHOON_API_KEY=

# OpenRouteService — ขอที่ https://openrouteservice.org  (ใช้ใน B3 routing เดิน)
ORS_API_KEY=
